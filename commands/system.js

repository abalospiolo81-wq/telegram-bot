module.exports = (bot) => {
  bot.start(ctx => ctx.reply("🔥 Bot LIVE! Type /help"));
  bot.command("help", ctx =>
    ctx.reply(
`📌 COMMAND LIST
/system
/fun
/group
/info
/games
/admin`
    )
  );
};
